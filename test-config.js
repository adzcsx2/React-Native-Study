// 测试 Tailwind 配置
const tailwindConfig = require("./tailwind.config.js");

console.log("Tailwind darkMode 配置:", tailwindConfig.darkMode);
console.log("配置对象:", JSON.stringify(tailwindConfig, null, 2));
